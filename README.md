# StreetCred Scaffold

This repository contains a minimal Turborepo monorepo setup.

## Setup

1. Copy `.env.example` to `.env` and adjust values as needed.
2. Start local Postgres and NATS using `docker/docker-compose.yml`.
3. Run `pnpm dev` to start all services.
