{
  "name": "streetcred",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "turbo": "^1.13.0"
  }
}
